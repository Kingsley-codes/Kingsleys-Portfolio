export interface Project {
  title: string;
  description: string;
  tags: string[];
  gradient: string;
  icon: React.ReactNode;
}

export interface TechStackItem {
  name: string;
  description: string;
  icon: React.ReactNode;
}
